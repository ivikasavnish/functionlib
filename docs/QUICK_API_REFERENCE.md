# FunctionLib - Quick API Reference

## All Available Functions

### Math

**algebra.py** (33 functions)

```python
from functionlib.math.algebra import (
    List,
    Optional,
    Tuple,
    Union,
    absolute_value,
    add_polynomials,
    arithmetic_sequence_sum,
    arithmetic_sequence_term,
    binary_logarithm,
    binomial_coefficient,
    # ... and 23 more
)
```

**calculus.py** (22 functions)

```python
from functionlib.math.calculus import (
    Callable,
    List,
    Optional,
    Tuple,
    arc_length,
    bisection_method,
    definite_integral_power,
    derivative_numerical,
    derivative_power_rule,
    euler_method,
    # ... and 12 more
)
```

**geometry.py** (36 functions)

```python
from functionlib.math.geometry import (
    List,
    Tuple,
    arc_length_circle,
    circle_area,
    circle_circumference,
    cone_surface_area,
    cone_volume,
    cube_surface_area,
    cube_volume,
    cylinder_surface_area,
    # ... and 26 more
)
```

**trigonometry.py** (24 functions)

```python
from functionlib.math.trigonometry import (
    Tuple,
    acos,
    acosh,
    asin,
    asinh,
    atan,
    atan2,
    atanh,
    cartesian_to_polar,
    cos,
    # ... and 14 more
)
```

**statistics.py** (22 functions)

```python
from functionlib.math.statistics import (
    List,
    Optional,
    Tuple,
    coefficient_of_variation,
    correlation_coefficient,
    covariance,
    geometric_mean,
    harmonic_mean,
    interquartile_range,
    kurtosis,
    # ... and 12 more
)
```

### Science

**physics.py** (32 functions)

```python
from functionlib.science.physics import (
    List,
    Tuple,
    acceleration,
    celsius_to_fahrenheit,
    celsius_to_kelvin,
    centripetal_acceleration,
    centripetal_force,
    coulomb_law,
    electrical_power,
    escape_velocity,
    # ... and 22 more
)
```

**chemistry.py** (24 functions)

```python
from functionlib.science.chemistry import (
    Dict,
    List,
    Tuple,
    arrhenius_equation,
    combined_gas_law,
    concentration_from_ph,
    dilution,
    empirical_formula_from_percent,
    enthalpy_change,
    equilibrium_constant,
    # ... and 14 more
)
```

**biology.py** (21 functions)

```python
from functionlib.science.biology import (
    Dict,
    List,
    Tuple,
    blood_alcohol_concentration,
    bmi,
    dna_complement,
    dna_reverse_complement,
    doubling_time,
    gc_content,
    hamming_distance,
    # ... and 11 more
)
```

### Coding

**data_structures.py** (16 functions)

```python
from functionlib.coding.data_structures import (
    Any,
    BinarySearchTree,
    BinaryTreeNode,
    Graph,
    LinkedList,
    LinkedListNode,
    List,
    Optional,
    PriorityQueue,
    Queue,
    # ... and 6 more
)
```

**algorithms.py** (24 functions)

```python
from functionlib.coding.algorithms import (
    Any,
    Callable,
    Dict,
    List,
    Optional,
    Set,
    Tuple,
    binary_search,
    breadth_first_search,
    bubble_sort,
    # ... and 14 more
)
```

**string_operations.py** (28 functions)

```python
from functionlib.coding.string_operations import (
    Dict,
    List,
    Optional,
    camel_case,
    character_frequency,
    contains_substring,
    count_consonants,
    count_vowels,
    ends_with,
    is_palindrome,
    # ... and 18 more
)
```

### General Purpose

**date_time.py** (29 functions)

```python
from functionlib.general_purpose.date_time import (
    Optional,
    Tuple,
    add_days,
    add_hours,
    add_minutes,
    age_in_years,
    current_datetime,
    current_timestamp,
    datetime,
    datetime_to_timestamp,
    # ... and 19 more
)
```

**string_utilities.py** (21 functions)

```python
from functionlib.general_purpose.string_utilities import (
    List,
    Optional,
    base64_decode,
    base64_encode,
    capitalize_words,
    escape_html,
    extract_emails,
    extract_numbers,
    extract_urls,
    levenshtein_distance,
    # ... and 11 more
)
```

**validation.py** (26 functions)

```python
from functionlib.general_purpose.validation import (
    Any,
    List,
    Optional,
    contains_digit,
    contains_lowercase,
    contains_special_char,
    contains_uppercase,
    is_alpha,
    is_alphanumeric,
    is_credit_card,
    # ... and 16 more
)
```


---

**Total: 395 functions/classes across 14 modules**
